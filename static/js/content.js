/* ============================================================
   content.js — legacy slide-module list
   The old example induction modules have been removed.
   Induction & Training content is now managed by the admin
   from the portal (uploaded as PDF/HTML via Google Drive),
   shown in the "Document modules" section of each tab.

   This file is kept (empty list) so the portal's older
   slide-based code has nothing to display and stays harmless.
   ============================================================ */

const INDUCTION = [];
